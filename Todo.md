-- Use context to persist the value of database dashboard sidebar
-- fix the flow of the other pages in database aside student
-- Lesson plan (3 pages) || timetable (2 pages)
-- Roles || Staff

--change the select to default html select
